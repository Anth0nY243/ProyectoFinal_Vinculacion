package modelo;

public class Enums {
    public enum RolUsuario { ADMINISTRADOR, COORDINADOR, VOLUNTARIO }
    public enum EstadoInscripcion { PENDIENTE, APROBADA, RECHAZADA, ASISTIO }
    public enum EstadoRequisito { PENDIENTE, APROBADO, RECHAZADO }
    public enum TipoRequisito { HERRAMIENTA, VESTIMENTA, HABILIDAD, DOCUMENTO, TRANSPORTE }
}